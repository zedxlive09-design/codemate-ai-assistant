[package]
name = "codemate-ai"
version = "1.0.0"
edition = "2021"

[lib]
name = "codemate_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
# llama.cpp integration for real LLM inference
llama_cpp_rs = "0.6"
walkdir = "2"
regex = "1"
glob = "0.3"
sysinfo = "0.32"
num_cpus = "1"
chrono = { version = "0.4", features = ["serde"] }
# Async channel for streaming
futures = "0.3"
tokio-stream = "0.1"
# UUID generation
uuid = { version = "1", features = ["v4"] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
