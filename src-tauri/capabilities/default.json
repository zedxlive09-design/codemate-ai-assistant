{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the CodeMate main window. Grants the minimum permissions the frontend needs: filesystem text IO (readTextFile/writeTextFile/exists/mkdir), file/folder dialogs (open/save), opening external links via the shell plugin (shell:allow-open), and the core:default set (which includes core:event:* used by `emit`/`listen` for the model:* and project:* custom events).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-exists",
    "fs:allow-mkdir",
    "shell:allow-open"
  ]
}
